#!/bin/sh
# Strip Cursor/agent attribution before the commit message is finalized.
MSG_FILE="$1"
[ -f "$MSG_FILE" ] || exit 0

tmp="${MSG_FILE}.no-cursor-attr.$$"
grep -viE '^(Co-authored-by:.*(cursor|cursoragent)|Made-with:.*cursor)' "$MSG_FILE" > "$tmp" || true
mv "$tmp" "$MSG_FILE"
